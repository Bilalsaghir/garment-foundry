/* This component was removed during a rollback. OneDrive blocked unlinking,
   so the empty stub remains. Not imported anywhere. Safe to delete manually. */
export default function _RemovedGarmentInteractive() { return null; }
